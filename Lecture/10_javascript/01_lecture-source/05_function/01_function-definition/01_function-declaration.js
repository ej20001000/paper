// 함수 선언문

function hello(name) {
    return `${name}님 환영합니다!`;
};

console.log(hello('오징어'));

var hello2 = function (name) {
    return `${name}님 안녕하세요!`;
};

console.log(hello2('유후'));
// console.log(hello3('유후')); 에러 뜸

