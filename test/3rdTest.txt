1. 5를 입력 받았을 때 프로그램의 결과를 쓰시오.

int main(){
    int num;
    scanf("%d",&num);

    switch(num){
        case 1:
            printf("1을 입력\n");
            break;
        case 2:
            printf("2을 입력\n");
            break;
        case 3:
            printf("3을 입력\n");
            break;
        default:
            printf("선택불가\n");
            break;
    }
}
    - 선택불가

2. class hrdKorea{
    public static void main(String[] args){
        int a=9;
        int b=11;
        int c=a^b;

        System.out.println(c);
    }
}
    - 2
    ** ^: XOR, 숫자는 2진수로 변환 후 비교
    9(2진수): 1001  11(2진수): 1011
    9^11 = 0010(2진수) = 2(10진수) **

3. 속성을 추가
alter table 테이블명 ( ) 주소 char(30);
    - add

4. 다음 빈칸에 들어갈 용어를 쓰시오.

데이터베이스: test00

student 테이블

no  name    phone       depart
1   이동국  111-2222    정처기
2   강은미  333-4444    정처기
3   김정미  555-6666    정처기
4   홍길동  777-8888    정처기
5   장길산  999-0000    정처기

score 테이블
no  score
1   80
2   90
3   95
4   90
5   85

select 이름, 점수
from test00.student, test00.score
( ) test00.student.no = test00.score.no

    - where
    ** join은 ,으로 대체 될 수 있음
    join일 시 on/,일 시 where로 조건절 사용 **

5. 안드로이드 코드네임 7은 Nougat, 8은 Oreoo, 최신 안드로이드인 9.x의 코드네임은 무엇인가?
    - 파이

6. rsh, rloging 대체, 22번 포트 네트워크 보안 도구 중 하나로 원격접속을 안전하게 할 수 있게 해주는 프로토콜
    - SSH
    ** SSH(Secure SHell): 네트워크 상의 다른 컴퓨터에 로그인하거나 원격 시스템에서 명령을 실행하고 다른 시스템으로 파일을 복사할 수 있도록 해주는 프로토콜 **
    [키워드: rsh, rloging, 22번 포트, 원격 접속 -> SSH]

7. 8개의 보기 중 DML 명령어 고르시오.
   grant, commit, select, savepoint,
   insert, create, alter, drop
    - select, insert

8. 스키마 모델링 순서
   ( )->( )->( )
    - 개념적설계, 논리적설계, 물리적설계
    [키워드: 개논물]

